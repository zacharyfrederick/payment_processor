[package]
name = "payments-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rust_decimal = { version = "1", features = ["serde"] }
thiserror = "1"

[dev-dependencies]
rust_decimal_macros = "1"
